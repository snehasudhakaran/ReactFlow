import React from 'react'

// Creating a context for data and its updater function
// This will be used to share the state across the components
export const ContextData = React.createContext();
export const EdgeContext = React.createContext();
export const SelectNodeContext = React.createContext();
export const AlertContext = React.createContext();